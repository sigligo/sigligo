name: Market Analyzer Bot

on:
  schedule:
    - cron: '*/30 * * * *'  # Run every 30 minutes
  workflow_dispatch:        # Allow manual button click

jobs:
  run-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Important for saving files

    env:
      POLYMARKET_API_KEY: ${{ secrets.POLYMARKET_API_KEY }}
      POLYMARKET_SECRET: ${{ secrets.POLYMARKET_SECRET }}
      POLYMARKET_PASSPHRASE: ${{ secrets.POLYMARKET_PASSPHRASE }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install requests pandas

      - name: Run Analysis
        run: python main.py

      - name: Save Results
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "Auto-update: Market data" || exit 0
          git push
